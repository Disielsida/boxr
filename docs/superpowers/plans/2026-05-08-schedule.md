# Расписание боёв — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** реализовать автогенерацию расписания боёв (день / время / ринг) с ручными правками и публичным экраном, по спеке `docs/superpowers/specs/2026-05-08-schedule-design.md`.

**Architecture:** расширение существующих моделей (`Tournament`, `Match`) и существующего модуля `MatchesModule` — новых модулей не появляется. Алгоритм планировщика выносится в чистую функцию `schedule-builder.ts` (jest unit-тесты), бизнес-логика в `MatchesService`, бэкенд-валидация через bash smoke. Фронт получает новый виджет `widgets/schedule-view/`, новую вкладку в `TournamentManagePage` и новую секцию в `PublicTournamentPage`.

**Tech Stack:** NestJS 10, Prisma 5 (PostgreSQL), class-validator, jest 29 (для schedule-builder), React 19 + react-router 7, Playwright 1.59 (e2e).

**Repository state:** не git-репозиторий. Шаги «Commit» пропускаются.

**Сервисы для интеграционных тестов:**
- Postgres работает в `boxr-postgres`. Если нет — `cd boxr-api && docker compose up -d`.
- `boxr-api` стартует через `npm run start:prod` или `npm run start:dev`.
- `boxr` стартует через `npm run dev`.

---

## Task 1: Prisma — добавить поля расписания у Tournament и Match

**Files:**
- Modify: `boxr-api/prisma/schema.prisma`
- Create: `boxr-api/prisma/migrations/<timestamp>_match_schedule/migration.sql` (через `prisma migrate dev`)

- [ ] **Step 1: Расширить модель `Tournament`**

В `boxr-api/prisma/schema.prisma` найти `model Tournament { ... }`. После существующих скалярных полей (но до relations) добавить:

```prisma
  ringCount       Int      @default(1)
  dayStartTime    String   @default("10:00")
  slotMinutes     Int      @default(30)
  minRestMinutes  Int      @default(60)
```

- [ ] **Step 2: Расширить модель `Match`**

В `model Match { ... }` после `endRound` (или рядом с другими `*At` полями) добавить:

```prisma
  scheduledAt   DateTime?
  ring          Int?
```

- [ ] **Step 3: Создать и применить миграцию**

```bash
cd /Users/andreisidorcenko/diplom/boxr-api && npm run prisma:migrate -- --name match_schedule
```

Expected: «Applying migration `<timestamp>_match_schedule`», prisma client регенерируется.

- [ ] **Step 4: Проверить генерацию client**

```bash
cd /Users/andreisidorcenko/diplom/boxr-api && npx prisma validate && npx prisma generate
```

Expected: оба без ошибок. Быстрая проверка:
```bash
grep -E "scheduledAt|ringCount" boxr-api/node_modules/.prisma/client/index.d.ts | head -5
```
должно показать упоминания.

---

## Task 2: schedule-builder.ts — чистая функция планировщика (TDD)

**Files:**
- Create: `boxr-api/src/matches/schedule-builder.ts`
- Create: `boxr-api/src/matches/schedule-builder.spec.ts`

`planSchedule` принимает входной массив матчей (без bye) и параметры турнира, возвращает либо массив назначений `(matchId, scheduledAt, ring)`, либо `{ error: string }`.

- [ ] **Step 1: Написать первый тест — два матча в один день, один ринг**

```ts
// boxr-api/src/matches/schedule-builder.spec.ts
import { planSchedule, PlannerInputMatch, PlannerParams } from './schedule-builder';

const baseParams: PlannerParams = {
  dateStart: '2099-06-14',     // ISO date YYYY-MM-DD
  dateEnd: '2099-06-14',
  ringCount: 1,
  dayStartTime: '10:00',
  slotMinutes: 30,
  minRestMinutes: 60,
  matchDuration: 11,           // 3*3 + 2 = 11
  dayEndTime: '22:00',
};

describe('planSchedule', () => {
  it('два независимых матча в один день, один ринг: 10:00 и 10:30', () => {
    const matches: PlannerInputMatch[] = [
      { id: 'M1', round: 1, position: 0, redBoxerId: 'A', blueBoxerId: 'B', prevMatchIds: [] },
      { id: 'M2', round: 1, position: 1, redBoxerId: 'C', blueBoxerId: 'D', prevMatchIds: [] },
    ];
    const result = planSchedule(matches, baseParams);
    expect('error' in result).toBe(false);
    if ('error' in result) return;
    expect(result.assignments).toHaveLength(2);
    expect(result.assignments[0]).toMatchObject({ matchId: 'M1', ring: 1, scheduledAt: '2099-06-14T10:00:00.000Z' });
    expect(result.assignments[1]).toMatchObject({ matchId: 'M2', ring: 1, scheduledAt: '2099-06-14T10:30:00.000Z' });
  });
});
```

- [ ] **Step 2: Запустить тест — должен упасть «module not found»**

```bash
cd /Users/andreisidorcenko/diplom/boxr-api && npx jest src/matches/schedule-builder.spec.ts
```
Expected: FAIL.

- [ ] **Step 3: Минимальная реализация**

```ts
// boxr-api/src/matches/schedule-builder.ts
export interface PlannerInputMatch {
  id: string;
  round: number;
  position: number;
  redBoxerId: string | null;
  blueBoxerId: string | null;
  prevMatchIds: string[];   // id-шники матчей-предшественников (по nextMatchId)
}

export interface PlannerParams {
  dateStart: string;        // 'YYYY-MM-DD'
  dateEnd: string;
  ringCount: number;
  dayStartTime: string;     // 'HH:MM'
  dayEndTime: string;       // 'HH:MM'
  slotMinutes: number;
  minRestMinutes: number;
  matchDuration: number;
}

export interface ScheduleAssignment {
  matchId: string;
  scheduledAt: string;      // ISO 8601 в UTC
  ring: number;
}

export type PlanResult = { assignments: ScheduleAssignment[] } | { error: string };

export function planSchedule(
  matches: readonly PlannerInputMatch[],
  params: PlannerParams,
): PlanResult {
  // Сортируем по (round asc, position asc) — гарантирует, что предшественники планируются первыми
  const sorted = [...matches].sort((a, b) => a.round - b.round || a.position - b.position);

  const days = enumerateDays(params.dateStart, params.dateEnd);
  const dayStartMin = parseHHMM(params.dayStartTime);
  const dayEndMin = parseHHMM(params.dayEndTime);

  // Состояние планировщика
  const ringBusy = new Map<string, true>();          // ключ "day:ring:slotStart"
  const matchEndById = new Map<string, number>();    // абсолютные минуты от 0
  const boxerLastFinish = new Map<string, number>(); // абсолютные минуты

  const assignments: ScheduleAssignment[] = [];

  for (const match of sorted) {
    const candidate = findEarliestSlot(match, days, dayStartMin, dayEndMin, params, ringBusy, matchEndById, boxerLastFinish, sorted);
    if (!candidate) {
      return { error: 'Не помещается в указанные даты — добавьте дни, ринги или уменьшите шаг между боями' };
    }
    const startAbsMin = candidate.dayIndex * 24 * 60 + candidate.slotMin;
    const endAbsMin = startAbsMin + params.matchDuration;
    matchEndById.set(match.id, endAbsMin);
    for (const boxerId of potentialBoxerIds(match, sorted)) {
      const prev = boxerLastFinish.get(boxerId) ?? -Infinity;
      if (endAbsMin > prev) boxerLastFinish.set(boxerId, endAbsMin);
    }
    ringBusy.set(`${candidate.dayIndex}:${candidate.ring}:${candidate.slotMin}`, true);

    const dateStr = days[candidate.dayIndex];
    const hh = String(Math.floor(candidate.slotMin / 60)).padStart(2, '0');
    const mm = String(candidate.slotMin % 60).padStart(2, '0');
    assignments.push({
      matchId: match.id,
      scheduledAt: `${dateStr}T${hh}:${mm}:00.000Z`,
      ring: candidate.ring,
    });
  }

  return { assignments };
}

function findEarliestSlot(
  match: PlannerInputMatch,
  days: string[],
  dayStartMin: number,
  dayEndMin: number,
  params: PlannerParams,
  ringBusy: Map<string, true>,
  matchEndById: Map<string, number>,
  boxerLastFinish: Map<string, number>,
  allMatches: readonly PlannerInputMatch[],
) {
  const predecessorEnds = match.prevMatchIds
    .map((pid) => matchEndById.get(pid))
    .filter((x): x is number => x !== undefined);
  const earliestByPredecessors = predecessorEnds.length === 0 ? -Infinity : Math.max(...predecessorEnds);

  const boxers = potentialBoxerIds(match, allMatches);
  const earliestByRest = boxers.reduce((acc, b) => {
    const last = boxerLastFinish.get(b);
    if (last === undefined) return acc;
    return Math.max(acc, last + params.minRestMinutes);
  }, -Infinity);

  const earliestAbsMin = Math.max(earliestByPredecessors, earliestByRest);

  for (let dayIndex = 0; dayIndex < days.length; dayIndex++) {
    for (let slotMin = dayStartMin; slotMin + params.matchDuration <= dayEndMin; slotMin += params.slotMinutes) {
      const absMin = dayIndex * 24 * 60 + slotMin;
      if (absMin < earliestAbsMin) continue;
      for (let ring = 1; ring <= params.ringCount; ring++) {
        if (ringBusy.has(`${dayIndex}:${ring}:${slotMin}`)) continue;
        return { dayIndex, slotMin, ring };
      }
    }
  }
  return null;
}

function potentialBoxerIds(match: PlannerInputMatch, all: readonly PlannerInputMatch[]): string[] {
  if (match.redBoxerId && match.blueBoxerId) {
    return [match.redBoxerId, match.blueBoxerId];
  }
  // Раунд ≥ 2: рекурсивно объединяем потенциальных участников из предшественников
  const result: string[] = [];
  for (const prevId of match.prevMatchIds) {
    const prev = all.find((m) => m.id === prevId);
    if (prev) {
      for (const b of potentialBoxerIds(prev, all)) {
        if (!result.includes(b)) result.push(b);
      }
    }
  }
  if (match.redBoxerId && !result.includes(match.redBoxerId)) result.push(match.redBoxerId);
  if (match.blueBoxerId && !result.includes(match.blueBoxerId)) result.push(match.blueBoxerId);
  return result;
}

function parseHHMM(s: string): number {
  const [h, m] = s.split(':').map(Number);
  return h * 60 + m;
}

function enumerateDays(start: string, end: string): string[] {
  const result: string[] = [];
  const d = new Date(start + 'T00:00:00.000Z');
  const last = new Date(end + 'T00:00:00.000Z');
  while (d <= last) {
    result.push(d.toISOString().slice(0, 10));
    d.setUTCDate(d.getUTCDate() + 1);
  }
  return result;
}
```

- [ ] **Step 4: Тест должен пройти**

```bash
cd /Users/andreisidorcenko/diplom/boxr-api && npx jest src/matches/schedule-builder.spec.ts
```
Expected: 1/1 PASS.

- [ ] **Step 5: Тесты на 2 ринга, отдых, размазывание по дням**

В тот же spec-файл добавить:

```ts
it('4 матча, 2 ринга, 1 день: 1/4 параллельно, полуфинал после отдыха', () => {
  const matches: PlannerInputMatch[] = [
    { id: 'Q1', round: 1, position: 0, redBoxerId: 'A', blueBoxerId: 'B', prevMatchIds: [] },
    { id: 'Q2', round: 1, position: 1, redBoxerId: 'C', blueBoxerId: 'D', prevMatchIds: [] },
    { id: 'S1', round: 2, position: 0, redBoxerId: null, blueBoxerId: null, prevMatchIds: ['Q1', 'Q2'] },
  ];
  const result = planSchedule(matches, { ...baseParams, ringCount: 2 });
  expect('error' in result).toBe(false);
  if ('error' in result) return;
  // Q1 и Q2 параллельно: оба в 10:00, разные ринги
  const q1 = result.assignments.find((a) => a.matchId === 'Q1')!;
  const q2 = result.assignments.find((a) => a.matchId === 'Q2')!;
  expect(q1.scheduledAt).toBe('2099-06-14T10:00:00.000Z');
  expect(q2.scheduledAt).toBe('2099-06-14T10:00:00.000Z');
  expect(q1.ring).not.toBe(q2.ring);
  // S1 после отдыха: 10:00 + 11 мин (matchDuration) + 60 мин (отдых) = 11:11 → ближайший слот ≥ 11:11 = 11:30
  const s1 = result.assignments.find((a) => a.matchId === 'S1')!;
  expect(s1.scheduledAt).toBe('2099-06-14T11:30:00.000Z');
});

it('1 ринг, мало времени → 422 «не помещается»', () => {
  const tooMany: PlannerInputMatch[] = Array.from({ length: 50 }, (_, i) => ({
    id: `M${i}`,
    round: 1,
    position: i,
    redBoxerId: `A${i}`,
    blueBoxerId: `B${i}`,
    prevMatchIds: [],
  }));
  const result = planSchedule(tooMany, baseParams);
  expect('error' in result).toBe(true);
  if ('error' in result) {
    expect(result.error).toContain('Не помещается');
  }
});

it('размазывание по 2 дням при 1 ринге', () => {
  // 8 простых матчей × 30 мин слот: с 10:00 по 22:00 в день влезает (22-10)*60/30 = 24 матча.
  // 8 матчей точно влезут в день. Тест на размазывание делаем на отдыхе через дерево:
  // полуфиналы и финал требуют отдыха, что может «уехать» за 22:00.
  const matches: PlannerInputMatch[] = [];
  // 4 четвертьфинала (Q0..Q3), 2 полуфинала (S0, S1), 1 финал (F)
  for (let i = 0; i < 4; i++) {
    matches.push({ id: `Q${i}`, round: 1, position: i, redBoxerId: `A${i}`, blueBoxerId: `B${i}`, prevMatchIds: [] });
  }
  matches.push({ id: 'S0', round: 2, position: 0, redBoxerId: null, blueBoxerId: null, prevMatchIds: ['Q0', 'Q1'] });
  matches.push({ id: 'S1', round: 2, position: 1, redBoxerId: null, blueBoxerId: null, prevMatchIds: ['Q2', 'Q3'] });
  matches.push({ id: 'F', round: 3, position: 0, redBoxerId: null, blueBoxerId: null, prevMatchIds: ['S0', 'S1'] });

  const result = planSchedule(matches, { ...baseParams, dateEnd: '2099-06-15', ringCount: 1 });
  expect('error' in result).toBe(false);
  if ('error' in result) return;
  expect(result.assignments).toHaveLength(7);
  // Все Q* в первом дне, F во втором (или конец первого, но нагрузка большая)
  const qs = result.assignments.filter((a) => a.matchId.startsWith('Q'));
  expect(qs.every((a) => a.scheduledAt.startsWith('2099-06-14'))).toBe(true);
});
```

- [ ] **Step 6: Прогон**

```bash
cd /Users/andreisidorcenko/diplom/boxr-api && npx jest src/matches/schedule-builder.spec.ts
```
Expected: 4/4 PASS.

- [ ] **Step 7: Тесты на инварианты**

Добавить:

```ts
it('инвариант: никакие два матча не делят (день, ринг, слот)', () => {
  const matches: PlannerInputMatch[] = Array.from({ length: 6 }, (_, i) => ({
    id: `M${i}`, round: 1, position: i,
    redBoxerId: `A${i}`, blueBoxerId: `B${i}`,
    prevMatchIds: [],
  }));
  const result = planSchedule(matches, { ...baseParams, ringCount: 2 });
  if ('error' in result) throw new Error(result.error);
  const seen = new Set<string>();
  for (const a of result.assignments) {
    const key = `${a.scheduledAt}:${a.ring}`;
    expect(seen.has(key)).toBe(false);
    seen.add(key);
  }
});

it('инвариант: предшественник заканчивается до начала следующего матча', () => {
  const matches: PlannerInputMatch[] = [
    { id: 'A', round: 1, position: 0, redBoxerId: 'X', blueBoxerId: 'Y', prevMatchIds: [] },
    { id: 'B', round: 1, position: 1, redBoxerId: 'Z', blueBoxerId: 'W', prevMatchIds: [] },
    { id: 'C', round: 2, position: 0, redBoxerId: null, blueBoxerId: null, prevMatchIds: ['A', 'B'] },
  ];
  const result = planSchedule(matches, { ...baseParams, ringCount: 2 });
  if ('error' in result) throw new Error(result.error);
  const a = result.assignments.find((x) => x.matchId === 'A')!;
  const c = result.assignments.find((x) => x.matchId === 'C')!;
  const aEnd = new Date(a.scheduledAt).getTime() + baseParams.matchDuration * 60_000;
  const cStart = new Date(c.scheduledAt).getTime();
  expect(cStart).toBeGreaterThanOrEqual(aEnd);
});

it('инвариант: боксёр отдыхает ≥ minRestMinutes между матчами', () => {
  // Один и тот же боксёр X в двух матчах раунда 1 — теоретически невозможно, но проверим
  // через цепочку: X — победитель Q0 (если Y слабый) и Q1, тогда S0(Q0,Q1) — между ними отдых
  const matches: PlannerInputMatch[] = [
    { id: 'Q0', round: 1, position: 0, redBoxerId: 'X', blueBoxerId: 'Y', prevMatchIds: [] },
    { id: 'Q1', round: 1, position: 1, redBoxerId: 'X', blueBoxerId: 'Z', prevMatchIds: [] },
    // ↑ невалидно для реальной сетки, но алгоритм-уровневый тест
  ];
  const result = planSchedule(matches, baseParams);
  if ('error' in result) throw new Error(result.error);
  const q0 = result.assignments.find((a) => a.matchId === 'Q0')!;
  const q1 = result.assignments.find((a) => a.matchId === 'Q1')!;
  const q0End = new Date(q0.scheduledAt).getTime() + baseParams.matchDuration * 60_000;
  const q1Start = new Date(q1.scheduledAt).getTime();
  expect(q1Start - q0End).toBeGreaterThanOrEqual(baseParams.minRestMinutes * 60_000);
});
```

- [ ] **Step 8: Финальный прогон**

```bash
cd /Users/andreisidorcenko/diplom/boxr-api && npx jest src/matches/schedule-builder.spec.ts
```
Expected: 7/7 PASS.

---

## Task 3: matches.service — generateSchedule + clearSchedule + setSchedule

**Files:**
- Modify: `boxr-api/src/matches/matches.service.ts`

- [ ] **Step 1: Добавить импорт `planSchedule` и helper для длительности**

В `boxr-api/src/matches/matches.service.ts` найти секцию импортов и добавить:

```ts
import { planSchedule, PlannerInputMatch } from './schedule-builder';
```

Перед `@Injectable()` (рядом с `nonByeCompletedFilter`) добавить:

```ts
function computeMatchDuration(rounds: number, roundDuration: number): number {
  return rounds * roundDuration + Math.max(0, rounds - 1);
}
```

- [ ] **Step 2: Метод `generateSchedule`**

В классе `MatchesService` добавить после `clearResult`:

```ts
async generateSchedule(userId: string, tournamentId: string): Promise<BracketResponse> {
  await this.prisma.$transaction(async (tx) => {
    const tournament = await tx.tournament.findUnique({ where: { id: tournamentId } });
    if (!tournament) throw new NotFoundException('Турнир не найден');
    if (tournament.organizerId !== userId) throw new ForbiddenException('Доступ запрещён');
    if (tournament.status !== TournamentStatus.IN_PROGRESS) {
      throw new UnprocessableEntityException('Расписание можно строить только для активного турнира');
    }
    const blocking = await tx.match.findFirst({
      where: { tournamentId, ...nonByeCompletedFilter },
    });
    if (blocking) {
      throw new UnprocessableEntityException('Нельзя пересчитать расписание: есть зафиксированные результаты');
    }

    // Грузим non-bye матчи. bye = COMPLETED + outcome=WO + один из слотов null
    const all = await tx.match.findMany({
      where: { tournamentId },
      orderBy: [{ round: 'asc' }, { position: 'asc' }],
    });
    const nonBye = all.filter(
      (m) => !(m.outcome === MatchOutcome.WO && (m.redBoxerId === null || m.blueBoxerId === null)),
    );
    if (nonBye.length === 0) {
      throw new UnprocessableEntityException('Нет матчей для расписания');
    }

    // Для каждого матча собираем prevMatchIds через nextMatchId-связь
    const prevByNext = new Map<string, string[]>();
    for (const m of all) {
      if (m.nextMatchId) {
        const arr = prevByNext.get(m.nextMatchId) ?? [];
        arr.push(m.id);
        prevByNext.set(m.nextMatchId, arr);
      }
    }
    const input: PlannerInputMatch[] = nonBye.map((m) => ({
      id: m.id,
      round: m.round,
      position: m.position,
      redBoxerId: m.redBoxerId,
      blueBoxerId: m.blueBoxerId,
      // Только non-bye предшественники: bye-матчи не «занимают» расписание,
      // но их боксёр уже продвинут в next, поэтому он учтён через redBoxerId/blueBoxerId.
      prevMatchIds: (prevByNext.get(m.id) ?? []).filter((pid) => {
        const p = all.find((x) => x.id === pid);
        return p && !(p.outcome === MatchOutcome.WO && (p.redBoxerId === null || p.blueBoxerId === null));
      }),
    }));

    const result = planSchedule(input, {
      dateStart: tournament.dateStart.toISOString().slice(0, 10),
      dateEnd: tournament.dateEnd.toISOString().slice(0, 10),
      ringCount: tournament.ringCount,
      dayStartTime: tournament.dayStartTime,
      dayEndTime: '22:00',
      slotMinutes: tournament.slotMinutes,
      minRestMinutes: tournament.minRestMinutes,
      matchDuration: computeMatchDuration(tournament.rounds, tournament.roundDuration),
    });
    if ('error' in result) {
      throw new UnprocessableEntityException(result.error);
    }

    // Сначала чистим всем (на случай если уже было расписание)
    await tx.match.updateMany({
      where: { tournamentId },
      data: { scheduledAt: null, ring: null },
    });
    for (const a of result.assignments) {
      await tx.match.update({
        where: { id: a.matchId },
        data: { scheduledAt: new Date(a.scheduledAt), ring: a.ring },
      });
    }
  });
  return this.buildBracketResponse(tournamentId);
}
```

- [ ] **Step 3: Метод `clearSchedule`**

Дальше в классе:

```ts
async clearSchedule(userId: string, tournamentId: string): Promise<BracketResponse> {
  await this.prisma.$transaction(async (tx) => {
    const tournament = await tx.tournament.findUnique({ where: { id: tournamentId } });
    if (!tournament) throw new NotFoundException('Турнир не найден');
    if (tournament.organizerId !== userId) throw new ForbiddenException('Доступ запрещён');
    if (tournament.status !== TournamentStatus.IN_PROGRESS) {
      throw new UnprocessableEntityException('Расписание можно сбрасывать только у активного турнира');
    }
    const blocking = await tx.match.findFirst({
      where: { tournamentId, ...nonByeCompletedFilter },
    });
    if (blocking) {
      throw new UnprocessableEntityException('Нельзя сбросить расписание: есть зафиксированные результаты');
    }
    await tx.match.updateMany({
      where: { tournamentId },
      data: { scheduledAt: null, ring: null },
    });
  });
  return this.buildBracketResponse(tournamentId);
}
```

- [ ] **Step 4: Метод `setSchedule`**

Дальше в классе:

```ts
async setSchedule(
  userId: string,
  matchId: string,
  scheduledAtIso: string,
  ring: number,
): Promise<BracketResponse> {
  const tournamentId = await this.prisma.$transaction(async (tx) => {
    const match = await tx.match.findUnique({
      where: { id: matchId },
      include: { tournament: true },
    });
    if (!match) throw new NotFoundException('Матч не найден');
    if (match.tournament.organizerId !== userId) {
      throw new ForbiddenException('Доступ запрещён');
    }
    if (match.status === MatchStatus.COMPLETED) {
      throw new UnprocessableEntityException('Зафиксированный матч не редактируется');
    }
    if (ring < 1 || ring > match.tournament.ringCount) {
      throw new UnprocessableEntityException(`Ринг ${ring} не существует у этого турнира`);
    }
    const newDate = new Date(scheduledAtIso);
    if (Number.isNaN(newDate.getTime())) {
      throw new UnprocessableEntityException('Невалидное время');
    }
    // Проверка диапазона: между dateStart 00:00 и dateEnd 22:00 включительно
    const tournamentStart = new Date(match.tournament.dateStart);
    const tournamentEndPlus = new Date(match.tournament.dateEnd);
    tournamentEndPlus.setUTCHours(22, 0, 0, 0);
    if (newDate < tournamentStart || newDate > tournamentEndPlus) {
      throw new UnprocessableEntityException('Время вне диапазона дат турнира');
    }
    // Слот не занят
    const conflict = await tx.match.findFirst({
      where: {
        tournamentId: match.tournamentId,
        scheduledAt: newDate,
        ring,
        NOT: { id: matchId },
      },
    });
    if (conflict) {
      throw new UnprocessableEntityException(`Ринг ${ring} занят в это время другим боем`);
    }
    // Порядок раундов: предшественники (с заполненным scheduledAt) должны закончиться до newDate
    const matchDuration = computeMatchDuration(match.tournament.rounds, match.tournament.roundDuration);
    const predecessors = await tx.match.findMany({
      where: { nextMatchId: matchId, scheduledAt: { not: null } },
    });
    for (const p of predecessors) {
      const pEnd = new Date(p.scheduledAt!.getTime() + matchDuration * 60_000);
      if (newDate < pEnd) {
        throw new UnprocessableEntityException(
          'Полуфинал/четвертьфинал, питающий этот матч, ещё не закончится к этому времени',
        );
      }
    }
    // Отдых: проверяем все матчи турнира с тем же боксёром
    const boxerIds = [match.redBoxerId, match.blueBoxerId].filter((x): x is string => x !== null);
    if (boxerIds.length > 0) {
      const others = await tx.match.findMany({
        where: {
          tournamentId: match.tournamentId,
          scheduledAt: { not: null },
          NOT: { id: matchId },
          OR: [
            { redBoxerId: { in: boxerIds } },
            { blueBoxerId: { in: boxerIds } },
          ],
        },
      });
      const restMs = match.tournament.minRestMinutes * 60_000;
      for (const o of others) {
        const oStart = o.scheduledAt!.getTime();
        const oEnd = oStart + matchDuration * 60_000;
        const newStart = newDate.getTime();
        const newEnd = newStart + matchDuration * 60_000;
        // Если матчи идут не одновременно — должны быть разнесены на minRest + matchDuration
        if (newStart < oEnd + restMs && oStart < newEnd + restMs) {
          throw new UnprocessableEntityException(
            `Боксёр должен отдыхать минимум ${match.tournament.minRestMinutes} минут между боями`,
          );
        }
      }
    }
    await tx.match.update({
      where: { id: matchId },
      data: { scheduledAt: newDate, ring },
    });
    return match.tournamentId;
  });
  return this.buildBracketResponse(tournamentId);
}
```

- [ ] **Step 5: Расширить `BracketResponseMatch` и mapping**

В файле найти `BracketResponseMatch` interface. Добавить два поля:

```ts
export interface BracketResponseMatch {
  // ... existing fields
  scheduledAt: string | null;
  ring: number | null;
  // ... existing fields like result
}
```

В функции `buildBracketResponse` найти `matches.map((m) => ({...}))` и в объект каждого матча добавить:

```ts
scheduledAt: m.scheduledAt ? m.scheduledAt.toISOString() : null,
ring: m.ring,
```

- [ ] **Step 6: Сборка**

```bash
cd /Users/andreisidorcenko/diplom/boxr-api && npm run build
```
Expected: clean.

---

## Task 4: matches.controller — 3 новых эндпоинта + DTO

**Files:**
- Create: `boxr-api/src/matches/dto/set-schedule.dto.ts`
- Modify: `boxr-api/src/matches/matches.controller.ts`

- [ ] **Step 1: DTO**

```ts
// boxr-api/src/matches/dto/set-schedule.dto.ts
import { IsInt, IsISO8601, Min } from 'class-validator';

export class SetMatchScheduleDto {
  @IsISO8601()
  scheduledAt!: string;

  @IsInt()
  @Min(1)
  ring!: number;
}
```

- [ ] **Step 2: Добавить эндпоинты в контроллер**

В `boxr-api/src/matches/matches.controller.ts` найти класс `MatchesController` и в конец класса (перед закрывающей `}`) добавить:

```ts
@UseGuards(JwtAuthGuard, RolesGuard)
@Roles(Role.ORGANIZER)
@Post('tournaments/:id/schedule')
@HttpCode(HttpStatus.OK)
generateSchedule(
  @CurrentUser() user: AuthUser,
  @Param('id', new ParseUUIDPipe()) tournamentId: string,
) {
  return this.service.generateSchedule(user.id, tournamentId);
}

@UseGuards(JwtAuthGuard, RolesGuard)
@Roles(Role.ORGANIZER)
@Delete('tournaments/:id/schedule')
@HttpCode(HttpStatus.OK)
clearSchedule(
  @CurrentUser() user: AuthUser,
  @Param('id', new ParseUUIDPipe()) tournamentId: string,
) {
  return this.service.clearSchedule(user.id, tournamentId);
}

@UseGuards(JwtAuthGuard, RolesGuard)
@Roles(Role.ORGANIZER)
@Patch('matches/:matchId/schedule')
setMatchSchedule(
  @CurrentUser() user: AuthUser,
  @Param('matchId', new ParseUUIDPipe()) matchId: string,
  @Body() dto: SetMatchScheduleDto,
) {
  return this.service.setSchedule(user.id, matchId, dto.scheduledAt, dto.ring);
}
```

И добавить импорт:
```ts
import { SetMatchScheduleDto } from './dto/set-schedule.dto';
```

- [ ] **Step 3: Сборка и проверка маршрутов**

```bash
cd /Users/andreisidorcenko/diplom/boxr-api && npm run build
```
Expected: clean.

Запустить API в отдельном терминале:
```bash
cd /Users/andreisidorcenko/diplom/boxr-api && npm run start:prod
```
Проверить что в стартовых логах есть строки про `POST /api/v1/tournaments/:id/schedule` и т.д. (NestJS RouterExplorer выводит их).

---

## Task 5: tournaments DTO/service — 4 новых поля турнира

**Files:**
- Modify: `boxr-api/src/tournaments/dto/create-tournament.dto.ts`
- Modify: `boxr-api/src/tournaments/dto/update-tournament.dto.ts` (если он не extends'ит create — иначе пропустить)
- Modify: `boxr-api/src/tournaments/tournaments.service.ts`

- [ ] **Step 1: Расширить CreateTournamentDto**

В `boxr-api/src/tournaments/dto/create-tournament.dto.ts` добавить в класс:

```ts
@IsOptional()
@IsInt()
@Min(1)
ringCount?: number;

@IsOptional()
@Matches(/^\d{2}:\d{2}$/, { message: 'dayStartTime должен быть в формате HH:MM' })
dayStartTime?: string;

@IsOptional()
@IsInt()
@Min(5)
slotMinutes?: number;

@IsOptional()
@IsInt()
@Min(0)
minRestMinutes?: number;
```

И импорты в шапке файла:
```ts
import { IsInt, IsOptional, Matches, Min } from 'class-validator';
```
(добавить недостающие к уже существующим импортам.)

- [ ] **Step 2: Проверить `UpdateTournamentDto`**

Если он extends'ит `PartialType(CreateTournamentDto)` (стандартный NestJS pattern) — никаких изменений не требуется, новые поля наследуются. Прочитать `boxr-api/src/tournaments/dto/update-tournament.dto.ts` и убедиться. Если нет — добавить те же 4 поля туда же.

- [ ] **Step 3: Проверить, что сервис прокидывает поля в Prisma**

Открыть `boxr-api/src/tournaments/tournaments.service.ts`. В методе `create` посмотреть, передаётся ли `dto` целиком в `prisma.tournament.create({data: ...})` или поля выбираются явно. Если явно — добавить новые. Если spread `...dto` — поля проходят сами (Prisma игнорирует undefined).

То же для `update`.

Аналогично, `findPublic` / `findMine` сейчас возвращают `tournament` целиком — новые поля автоматически попадают в ответ. Никаких изменений не требуется.

- [ ] **Step 4: Сборка**

```bash
cd /Users/andreisidorcenko/diplom/boxr-api && npm run build
```
Expected: clean.

---

## Task 6: bash smoke `scripts/test-schedule.sh`

**Files:**
- Create: `boxr-api/scripts/test-schedule.sh`

- [ ] **Step 1: Создать скрипт**

```bash
#!/usr/bin/env bash
# boxr-api/scripts/test-schedule.sh
# Интеграционный smoke по спеке docs/superpowers/specs/2026-05-08-schedule-design.md.
# Требует запущенный API на http://localhost:3000/api/v1.

set -u
API="${API:-http://localhost:3000/api/v1}"
STAMP=$(date +%s%N)$$
PASS=0; FAIL=0; RESULTS=()

record() {
  local name="$1" got="$2" want="$3"
  if [ "$got" = "$want" ]; then PASS=$((PASS+1)); RESULTS+=("OK   $name (got=$got)")
  else FAIL=$((FAIL+1)); RESULTS+=("FAIL $name (want=$want got=$got)"); fi
}
post() {
  local m="$1" u="$2" t="${3:-}" b="${4:-}"
  local args=(-s -o /tmp/boxr-body.json -w "%{http_code}" -X "$m" "$API$u")
  [ -n "$t" ] && args+=(-H "Authorization: Bearer $t")
  [ -n "$b" ] && args+=(-H "Content-Type: application/json" -d "$b")
  curl "${args[@]}"
}
json() { python3 -c "import sys,json;d=json.load(open('/tmp/boxr-body.json'));print(d$1)"; }
snap() { cp /tmp/boxr-body.json /tmp/boxr-snap.json; }
jsnap() { python3 -c "import sys,json;d=json.load(open('/tmp/boxr-snap.json'));print(d$1)"; }

# 1) Setup: 3 дня, 2 ринга, 4 боксёра в категории 71
post POST /auth/register "" "{\"email\":\"tr-$STAMP@boxr.test\",\"password\":\"Strong1pw\",\"fullName\":\"Tr\",\"role\":\"TRAINER\"}" >/dev/null
TR=$(json '["accessToken"]')
post POST /auth/register "" "{\"email\":\"org-$STAMP@boxr.test\",\"password\":\"Strong1pw\",\"fullName\":\"Org\",\"role\":\"ORGANIZER\"}" >/dev/null
ORG=$(json '["accessToken"]')

post POST /tournaments "$ORG" '{"name":"ScheduleTest","type":"REGIONAL","level":"AMATEUR","dateStart":"2099-06-14","dateEnd":"2099-06-16","city":"Москва","categories":[71],"rounds":3,"roundDuration":3,"helmets":false,"ringCount":2}' >/dev/null
T_ID=$(json '["id"]')
post POST "/tournaments/$T_ID/publish" "$ORG" >/dev/null

declare -a BOXERS
for i in 1 2 3 4; do
  post POST /boxers "$TR" "{\"fullName\":\"Боксёр $i $STAMP\",\"dob\":\"2000-01-15\",\"gender\":\"MALE\",\"weight\":71}" >/dev/null
  BOXERS+=("$(json '["id"]')")
done
declare -a APPS
for B in "${BOXERS[@]}"; do
  post POST /applications "$TR" "{\"tournamentId\":\"$T_ID\",\"items\":[{\"boxerId\":\"$B\",\"category\":71}]}" >/dev/null
  APPS+=("$(json '["items"][0]["id"]')")
done
for A in "${APPS[@]}"; do
  post POST "/applications/$A/approve" "$ORG" >/dev/null
done
post POST "/tournaments/$T_ID/bracket" "$ORG" >/dev/null

# 2) Генерируем расписание
status=$(post POST "/tournaments/$T_ID/schedule" "$ORG")
record "generate schedule" "$status" "200"
snap
ALL_HAVE_TIME=$(jsnap '["categories"][0]["matches"]' | python3 -c "import json,sys;ms=json.loads(sys.stdin.read().replace(chr(39),chr(34)));print('yes' if all(m.get('scheduledAt') is not None for m in ms) else 'no')" 2>/dev/null || echo "?")
# Альтернатива — вытащить через прямой json:
NUM=$(jsnap '["categories"][0]["matches"].__len__()')
record "matches count" "$NUM" "3"
HAS_RING=$(jsnap '["categories"][0]["matches"][0]["ring"]')
[ "$HAS_RING" != "None" ] && record "first match has ring" "yes" "yes" || record "first match has ring" "no" "yes"

# 3) Получить id первого матча и попробовать конфликтующий PATCH
M0=$(jsnap "['categories'][0]['matches'][0]['id']")
M1=$(jsnap "['categories'][0]['matches'][1]['id']")
TIME0=$(jsnap "['categories'][0]['matches'][0]['scheduledAt']")
RING0=$(jsnap "['categories'][0]['matches'][0]['ring']")
# Поставить M1 в тот же слот, что M0 → 422
status=$(post PATCH "/matches/$M1/schedule" "$ORG" "{\"scheduledAt\":\"$TIME0\",\"ring\":$RING0}")
record "conflict slot 422" "$status" "422"

# 4) Зафиксировать первый матч (он должен быть READY)
post PATCH "/matches/$M0" "$ORG" '{"winner":"RED","outcome":"WP"}' >/dev/null

# 5) Попытка перегенерировать после фиксации → 422
status=$(post POST "/tournaments/$T_ID/schedule" "$ORG")
record "regen blocked" "$status" "422"

# 6) Попытка clear после фиксации → 422
status=$(post DELETE "/tournaments/$T_ID/schedule" "$ORG")
record "clear blocked" "$status" "422"

echo
for r in "${RESULTS[@]}"; do echo "$r"; done
echo
echo "PASS=$PASS FAIL=$FAIL"
[ "$FAIL" = "0" ]
```

- [ ] **Step 2: Сделать исполняемым**

```bash
chmod +x /Users/andreisidorcenko/diplom/boxr-api/scripts/test-schedule.sh
```

- [ ] **Step 3: Запустить smoke**

API должен работать. В отдельном терминале:
```bash
cd /Users/andreisidorcenko/diplom/boxr-api && npm run start:dev
```

Когда API готов:
```bash
cd /Users/andreisidorcenko/diplom/boxr-api && ./scripts/test-schedule.sh
```

Expected: `PASS=5 FAIL=0`, exit 0.

---

## Task 7: Frontend types + matchesApi

**Files:**
- Modify: `boxr/src/shared/types/index.ts`
- Modify: `boxr/src/shared/api/matches.ts`
- Modify: `boxr/src/shared/api/tournaments.ts`

- [ ] **Step 1: Расширить тип `Tournament`**

В `boxr/src/shared/types/index.ts` найти `interface Tournament { ... }` и добавить:

```ts
ringCount: number;
dayStartTime: string;
slotMinutes: number;
minRestMinutes: number;
```

- [ ] **Step 2: Расширить `BracketMatch`**

В том же файле найти `interface BracketMatch { ... }` и добавить:

```ts
scheduledAt: string | null;
ring: number | null;
```

- [ ] **Step 3: Расширить `matchesApi`**

В `boxr/src/shared/api/matches.ts` в `ApiMatch` тип добавить:

```ts
scheduledAt: string | null;
ring: number | null;
```

В функции `toMatch` добавить эти два поля в возвращаемый объект:

```ts
scheduledAt: m.scheduledAt,
ring: m.ring,
```

И добавить три новых метода в `matchesApi`:

```ts
generateSchedule: (tournamentId: string) =>
  request<ApiBracket>(`/tournaments/${tournamentId}/schedule`, { method: 'POST' }).then(toBracket),
clearSchedule: (tournamentId: string) =>
  request<ApiBracket>(`/tournaments/${tournamentId}/schedule`, { method: 'DELETE' }).then(toBracket),
setSchedule: (matchId: string, scheduledAt: string, ring: number) =>
  request<ApiBracket>(`/matches/${matchId}/schedule`, {
    method: 'PATCH',
    body: { scheduledAt, ring },
  }).then(toBracket),
```

- [ ] **Step 4: Расширить тип `Tournament` в `tournaments.ts` (raw + mapping)**

В `boxr/src/shared/api/tournaments.ts` есть `RawTournament` и `mapTournament`. Прочитать файл и:

В `RawTournament` добавить:
```ts
ringCount: number;
dayStartTime: string;
slotMinutes: number;
minRestMinutes: number;
```

В `mapTournament` (и `mapPublic`, если он не наследует) — прокинуть:
```ts
ringCount: raw.ringCount,
dayStartTime: raw.dayStartTime,
slotMinutes: raw.slotMinutes,
minRestMinutes: raw.minRestMinutes,
```

- [ ] **Step 5: Проверить tsc**

```bash
cd /Users/andreisidorcenko/diplom/boxr && npx tsc -b --noEmit
```
Expected: clean.

---

## Task 8: widget schedule-view

**Files:**
- Create: `boxr/src/widgets/schedule-view/index.ts`
- Create: `boxr/src/widgets/schedule-view/ui/ScheduleView.tsx`
- Create: `boxr/src/widgets/schedule-view/ui/MatchScheduleDialog.tsx`

Стилистика — по образцу мокапа `index.html` секция `ScheduleTab` (строки 2841-2902).

- [ ] **Step 1: ScheduleView.tsx**

```tsx
// boxr/src/widgets/schedule-view/ui/ScheduleView.tsx
import { useMemo, useState } from 'react';
import type { Bracket, BracketMatch, Tournament } from '@/shared/types';
import { MonoLabel } from '@/shared/ui';

interface Props {
  bracket: Bracket;
  tournament?: Tournament;       // нужен только для приватного режима (для диалога)
  readOnly?: boolean;
  onMatchClick?: (match: BracketMatch) => void;
}

interface FlatMatch {
  match: BracketMatch;
  category: number;
  bout: number;
}

export const ScheduleView = ({ bracket, readOnly, onMatchClick }: Props) => {
  const flat = useMemo<FlatMatch[]>(() => {
    const arr: { match: BracketMatch; category: number }[] = [];
    for (const cat of bracket.categories) {
      for (const m of cat.matches) {
        if (m.scheduledAt !== null) arr.push({ match: m, category: cat.weight });
      }
    }
    arr.sort((a, b) =>
      a.match.scheduledAt!.localeCompare(b.match.scheduledAt!) ||
      (a.match.ring ?? 0) - (b.match.ring ?? 0),
    );
    return arr.map((x, i) => ({ ...x, bout: i + 1 }));
  }, [bracket]);

  const days = useMemo<string[]>(() => {
    const set = new Set<string>();
    for (const f of flat) set.add(f.match.scheduledAt!.slice(0, 10));
    return [...set].sort();
  }, [flat]);

  const [activeDay, setActiveDay] = useState<string | null>(null);
  const day = activeDay ?? days[0] ?? null;

  if (flat.length === 0) {
    return (
      <div style={{ padding: 40, textAlign: 'center', color: 'var(--ink-500)' }}>
        Расписание ещё не сгенерировано.
      </div>
    );
  }

  const dayMatches = day ? flat.filter((f) => f.match.scheduledAt!.startsWith(day)) : [];

  const formatTime = (iso: string): string => iso.slice(11, 16);
  const formatDay = (iso: string): string => {
    const d = new Date(iso + 'T00:00:00.000Z');
    return d.toLocaleDateString('ru-RU', { day: 'numeric', month: 'long', timeZone: 'UTC' });
  };

  return (
    <div>
      <div style={{ display: 'flex', gap: 0, marginBottom: 24, borderBottom: '1px solid var(--paper-300)' }}>
        {days.map((d) => (
          <button
            key={d}
            onClick={() => setActiveDay(d)}
            style={{
              padding: '10px 24px',
              background: 'none',
              border: 'none',
              borderBottom: d === day ? '2px solid var(--ink-900)' : '2px solid transparent',
              color: d === day ? 'var(--ink-900)' : 'var(--ink-500)',
              fontFamily: 'var(--font-mono)',
              fontSize: 'var(--text-sm)',
              cursor: 'pointer',
              marginBottom: -1,
              letterSpacing: '0.04em',
            }}
          >
            {formatDay(d)}
          </button>
        ))}
      </div>
      <div>
        {dayMatches.map(({ match, category, bout }) => {
          const clickable = !readOnly && onMatchClick && match.status !== 'completed';
          return (
            <div
              key={match.id}
              onClick={clickable ? () => onMatchClick(match) : undefined}
              style={{
                display: 'grid',
                gridTemplateColumns: '80px 80px 100px 1fr 30px 1fr 100px',
                alignItems: 'center',
                gap: 16,
                padding: '14px 0',
                borderBottom: '1px solid var(--paper-300)',
                cursor: clickable ? 'pointer' : 'default',
              }}
            >
              <div style={{ fontFamily: 'var(--font-mono)', fontSize: 'var(--text-base)' }}>
                {formatTime(match.scheduledAt!)}
              </div>
              <MonoLabel>Бой {bout}</MonoLabel>
              <MonoLabel>Ринг {match.ring}</MonoLabel>
              <div style={{ textAlign: 'right', fontWeight: 500 }}>
                {match.red?.fullName ?? <em style={{ color: 'var(--ink-300)' }}>—</em>}
              </div>
              <div style={{ textAlign: 'center', fontStyle: 'italic', color: 'var(--ink-300)' }}>vs</div>
              <div style={{ fontWeight: 500 }}>
                {match.blue?.fullName ?? <em style={{ color: 'var(--ink-300)' }}>—</em>}
              </div>
              <MonoLabel>{category} кг</MonoLabel>
            </div>
          );
        })}
      </div>
    </div>
  );
};
```

- [ ] **Step 2: MatchScheduleDialog.tsx**

```tsx
// boxr/src/widgets/schedule-view/ui/MatchScheduleDialog.tsx
import { useState } from 'react';
import type { BracketMatch, Tournament } from '@/shared/types';
import { matchesApi, ApiError } from '@/shared/api';
import { Button, MonoLabel } from '@/shared/ui';

interface Props {
  match: BracketMatch;
  tournament: Tournament;
  onClose: () => void;
  onResult: (bracket: Awaited<ReturnType<typeof matchesApi.setSchedule>>) => void;
}

export const MatchScheduleDialog = ({ match, tournament, onClose, onResult }: Props) => {
  const initialIso = match.scheduledAt ?? `${tournament.dateStart}T${tournament.dayStartTime}:00.000Z`;
  const [day, setDay] = useState(initialIso.slice(0, 10));
  const [time, setTime] = useState(initialIso.slice(11, 16));
  const [ring, setRing] = useState(match.ring ?? 1);
  const [submitting, setSubmitting] = useState(false);
  const [error, setError] = useState<string | null>(null);

  const days = enumerateDays(tournament.dateStart, tournament.dateEnd);
  const slots = enumerateSlots(tournament.dayStartTime, '22:00', tournament.slotMinutes);

  const submit = async () => {
    setSubmitting(true);
    setError(null);
    try {
      const scheduledAt = `${day}T${time}:00.000Z`;
      const fresh = await matchesApi.setSchedule(match.id, scheduledAt, ring);
      onResult(fresh);
      onClose();
    } catch (e) {
      setError(e instanceof ApiError ? e.message : 'Не удалось сохранить');
    } finally {
      setSubmitting(false);
    }
  };

  return (
    <div
      onClick={onClose}
      style={{
        position: 'fixed', inset: 0, background: 'rgba(26,20,61,0.35)',
        display: 'flex', alignItems: 'center', justifyContent: 'center', zIndex: 500,
      }}
    >
      <div
        onClick={(e) => e.stopPropagation()}
        style={{
          background: 'var(--paper-100)', padding: 28, borderRadius: 'var(--radius-md)',
          width: 360, maxWidth: '90vw', display: 'flex', flexDirection: 'column', gap: 16,
        }}
      >
        <MonoLabel>ПЕРЕНЕСТИ МАТЧ</MonoLabel>
        <div style={{ fontSize: 'var(--text-sm)' }}>
          🔴 {match.red?.fullName ?? '—'} vs 🔵 {match.blue?.fullName ?? '—'}
        </div>
        <label style={{ display: 'flex', flexDirection: 'column', gap: 4 }}>
          <MonoLabel>ДЕНЬ</MonoLabel>
          <select value={day} onChange={(e) => setDay(e.target.value)} style={{ padding: 8 }}>
            {days.map((d) => (
              <option key={d} value={d}>{d}</option>
            ))}
          </select>
        </label>
        <label style={{ display: 'flex', flexDirection: 'column', gap: 4 }}>
          <MonoLabel>ВРЕМЯ</MonoLabel>
          <select value={time} onChange={(e) => setTime(e.target.value)} style={{ padding: 8 }}>
            {slots.map((s) => (
              <option key={s} value={s}>{s}</option>
            ))}
          </select>
        </label>
        <label style={{ display: 'flex', flexDirection: 'column', gap: 4 }}>
          <MonoLabel>РИНГ</MonoLabel>
          <select value={ring} onChange={(e) => setRing(Number(e.target.value))} style={{ padding: 8 }}>
            {Array.from({ length: tournament.ringCount }, (_, i) => i + 1).map((r) => (
              <option key={r} value={r}>Ринг {r}</option>
            ))}
          </select>
        </label>
        {error && <div style={{ color: 'var(--danger)', fontSize: 'var(--text-sm)' }}>{error}</div>}
        <div style={{ display: 'flex', gap: 8, justifyContent: 'flex-end' }}>
          <Button variant="ghost" onClick={onClose} disabled={submitting}>Отмена</Button>
          <Button onClick={submit} disabled={submitting}>
            {submitting ? 'Сохраняем…' : 'Сохранить'}
          </Button>
        </div>
      </div>
    </div>
  );
};

function enumerateDays(start: string, end: string): string[] {
  const result: string[] = [];
  const d = new Date(start.slice(0, 10) + 'T00:00:00.000Z');
  const last = new Date(end.slice(0, 10) + 'T00:00:00.000Z');
  while (d <= last) {
    result.push(d.toISOString().slice(0, 10));
    d.setUTCDate(d.getUTCDate() + 1);
  }
  return result;
}

function enumerateSlots(start: string, end: string, step: number): string[] {
  const [sh, sm] = start.split(':').map(Number);
  const [eh, em] = end.split(':').map(Number);
  const startMin = sh * 60 + sm;
  const endMin = eh * 60 + em;
  const result: string[] = [];
  for (let m = startMin; m <= endMin; m += step) {
    const hh = String(Math.floor(m / 60)).padStart(2, '0');
    const mm = String(m % 60).padStart(2, '0');
    result.push(`${hh}:${mm}`);
  }
  return result;
}
```

- [ ] **Step 3: index.ts**

```ts
// boxr/src/widgets/schedule-view/index.ts
export { ScheduleView } from './ui/ScheduleView';
export { MatchScheduleDialog } from './ui/MatchScheduleDialog';
```

- [ ] **Step 4: tsc**

```bash
cd /Users/andreisidorcenko/diplom/boxr && npx tsc -b --noEmit
```
Expected: clean.

---

## Task 9: Расширить TournamentManagePage

**Files:**
- Modify: `boxr/src/pages/tournament-manage/ui/TournamentManagePage.tsx`

- [ ] **Step 1: Расширить `Tab`**

Найти строку:
```ts
type Tab = 'info' | 'participants' | 'bracket' | 'results';
```
Заменить на:
```ts
type Tab = 'info' | 'participants' | 'bracket' | 'schedule' | 'results';
```

И в массиве map для tab-ов:
```ts
(['info', 'participants', 'bracket', 'schedule', 'results'] as Tab[]).map((id) => (
```
Расширить тернарку лейблов:
```tsx
{id === 'info'
  ? 'Информация'
  : id === 'participants'
    ? 'Участники'
    : id === 'bracket'
      ? 'Жеребьёвка'
      : id === 'schedule'
        ? 'Расписание'
        : 'Результаты'}
```

- [ ] **Step 2: Импорты и состояние диалога**

В шапку файла добавить (если ещё нет):
```ts
import { ScheduleView, MatchScheduleDialog } from '@/widgets/schedule-view';
```

В состояние компонента (рядом с `activeMatch`) добавить:
```ts
const [scheduleMatch, setScheduleMatch] = useState<BracketMatch | null>(null);
```

- [ ] **Step 3: Рендер вкладки `schedule`**

Найти блок `{tab === 'results' && results && <ResultsView results={results} />}` и **перед** ним добавить:

```tsx
{tab === 'schedule' && tournament && bracket && (
  <ScheduleTab
    tournament={tournament}
    bracket={bracket}
    onMatchClick={setScheduleMatch}
    onGenerate={async () => {
      try {
        const b = await matchesApi.generateSchedule(tournament.id);
        setBracket(b);
      } catch (e) {
        setBracketError(e instanceof ApiError ? e.message : 'Ошибка');
      }
    }}
    onClear={async () => {
      try {
        const b = await matchesApi.clearSchedule(tournament.id);
        setBracket(b);
      } catch (e) {
        setBracketError(e instanceof ApiError ? e.message : 'Ошибка');
      }
    }}
  />
)}
{tab === 'schedule' && tournament && !bracket && (
  <div style={{ padding: 24, color: 'var(--ink-500)' }}>
    Сначала сгенерируйте сетку.
  </div>
)}
{scheduleMatch && tournament && (
  <MatchScheduleDialog
    match={scheduleMatch}
    tournament={tournament}
    onClose={() => setScheduleMatch(null)}
    onResult={(b) => setBracket(b)}
  />
)}
```

- [ ] **Step 4: Реализация `ScheduleTab`**

В конец файла (рядом с `BracketTab`) добавить:

```tsx
const ScheduleTab = ({
  tournament, bracket, onMatchClick, onGenerate, onClear,
}: {
  tournament: Tournament;
  bracket: Bracket;
  onMatchClick: (m: BracketMatch) => void;
  onGenerate: () => void;
  onClear: () => void;
}) => {
  const hasSchedule = bracket.categories.some((c) => c.matches.some((m) => m.scheduledAt !== null));
  const canMutate =
    tournament.status === 'in_progress' &&
    bracket.categories
      .flatMap((c) => c.matches)
      .every(
        (m) =>
          m.status !== 'completed' ||
          (m.result?.outcome === 'wo' && (m.red === null || m.blue === null)),
      );

  if (!hasSchedule) {
    return (
      <div style={{ padding: 24, textAlign: 'center' }}>
        <p style={{ marginBottom: 16, color: 'var(--ink-500)' }}>
          Расписание ещё не построено. Алгоритм распределит матчи по дням, рингам и времени.
        </p>
        {canMutate && <Button onClick={onGenerate}>Авто-расставить</Button>}
        {!canMutate && (
          <div style={{ color: 'var(--ink-500)' }}>
            Турнир должен быть активным и без зафиксированных результатов.
          </div>
        )}
      </div>
    );
  }

  return (
    <div>
      {canMutate && (
        <div style={{ marginBottom: 16, display: 'flex', justifyContent: 'flex-end', gap: 8 }}>
          <Button variant="ghost" onClick={onGenerate}>Перерасставить</Button>
          <Button variant="ghost" onClick={onClear}>Очистить расписание</Button>
        </div>
      )}
      <ScheduleView bracket={bracket} tournament={tournament} onMatchClick={onMatchClick} />
    </div>
  );
};
```

Импортировать `Tournament` тип в начале файла из `@/shared/types`, если ещё нет.

- [ ] **Step 5: tsc + lint**

```bash
cd /Users/andreisidorcenko/diplom/boxr && npx tsc -b --noEmit
```
Expected: clean.

---

## Task 10: Расширить PublicTournamentPage

**Files:**
- Modify: `boxr/src/pages/public-tournament/ui/PublicTournamentPage.tsx`

- [ ] **Step 1: Импорт + рендер секции**

Прочитать текущий файл. Добавить импорт:

```ts
import { ScheduleView } from '@/widgets/schedule-view';
```

Найти блок где рендерится `<BracketView bracket={bracket} readOnly />` и **после** него (но **перед** секцией результатов) добавить:

```tsx
{bracket && bracket.categories.some((c) => c.matches.some((m) => m.scheduledAt !== null)) && (
  <section style={{ marginBottom: 32 }}>
    <h2>Расписание</h2>
    <ScheduleView bracket={bracket} readOnly />
  </section>
)}
```

- [ ] **Step 2: tsc**

```bash
cd /Users/andreisidorcenko/diplom/boxr && npx tsc -b --noEmit
```
Expected: clean.

---

## Task 11: Playwright e2e schedule.spec.ts

**Files:**
- Create: `boxr/e2e/schedule.spec.ts`

- [ ] **Step 1: Написать spec**

```ts
// boxr/e2e/schedule.spec.ts
import { expect, test } from '@playwright/test';
import {
  approveApplicationViaApi,
  createBoxerViaApi,
  createTournamentViaApi,
  registerUser,
  seedTokens,
  submitApplicationViaApi,
} from './helpers';

test('организатор автогенерирует расписание и видит его на вкладке', async ({ page, request }) => {
  const organizer = await registerUser(request, 'ORGANIZER');
  const trainer = await registerUser(request, 'TRAINER');

  const tournament = await createTournamentViaApi(request, organizer, {
    status: 'PUBLISHED',
    categories: [71],
  });

  // 4 боксёра
  for (let i = 1; i <= 4; i++) {
    const b = await createBoxerViaApi(request, trainer, { fullName: `Sched ${i}`, weight: 71 });
    const app = await submitApplicationViaApi(request, trainer, tournament.id, b.id, 71);
    await approveApplicationViaApi(request, organizer, app.id);
  }

  // Сетку генерим через API
  const apiPort = process.env.E2E_API_PORT ?? 3000;
  const r = await request.post(
    `http://localhost:${apiPort}/api/v1/tournaments/${tournament.id}/bracket`,
    { headers: { Authorization: `Bearer ${organizer.accessToken}` } },
  );
  expect(r.ok()).toBeTruthy();

  // Логинимся в браузер и идём в управление
  await seedTokens(page, organizer);
  await page.goto(`/tournaments/${tournament.id}`);

  // На вкладку Расписание
  await page.getByRole('button', { name: 'Расписание' }).click();
  await page.getByRole('button', { name: 'Авто-расставить' }).click();

  // Видим хотя бы одну строку с временем (HH:MM формат)
  await expect(page.getByText(/\d{2}:\d{2}/)).toBeVisible({ timeout: 10_000 });
  // Виден переключатель дней — хотя бы одна кнопка с месяцем
  await expect(page.getByRole('button', { name: /июня|июля|января|февраля/ }).first()).toBeVisible();
});

test('публичная страница показывает расписание после автогенерации', async ({ page, request }) => {
  const organizer = await registerUser(request, 'ORGANIZER');
  const trainer = await registerUser(request, 'TRAINER');
  const tournament = await createTournamentViaApi(request, organizer, {
    status: 'PUBLISHED',
    categories: [60],
  });

  for (let i = 0; i < 2; i++) {
    const b = await createBoxerViaApi(request, trainer, { fullName: `P${i}`, weight: 60 });
    const app = await submitApplicationViaApi(request, trainer, tournament.id, b.id, 60);
    await approveApplicationViaApi(request, organizer, app.id);
  }

  const apiPort = process.env.E2E_API_PORT ?? 3000;
  const headers = { Authorization: `Bearer ${organizer.accessToken}` };
  await request.post(`http://localhost:${apiPort}/api/v1/tournaments/${tournament.id}/bracket`, { headers });
  await request.post(`http://localhost:${apiPort}/api/v1/tournaments/${tournament.id}/schedule`, { headers });

  await page.goto(`/public/tournaments/${tournament.id}`);
  await expect(page.getByRole('heading', { name: 'Расписание' })).toBeVisible();
  await expect(page.getByText(/\d{2}:\d{2}/)).toBeVisible();
});
```

- [ ] **Step 2: Запустить e2e**

API должен работать. В отдельном терминале (или Playwright сам поднимет):
```bash
cd /Users/andreisidorcenko/diplom/boxr && npm run test:e2e -- schedule.spec.ts
```

Expected: 2/2 PASS.

---

## Self-review checklist

После всех задач:

- [ ] `cd boxr-api && npm run build` — clean.
- [ ] `cd boxr-api && npx jest src/matches/` — все тесты bracket-builder + schedule-builder зелёные.
- [ ] `cd boxr-api && ./scripts/test-bracket.sh` — старый smoke не сломан.
- [ ] `cd boxr-api && ./scripts/test-schedule.sh` — `PASS=5 FAIL=0`.
- [ ] `cd boxr && npx tsc -b --noEmit` — clean.
- [ ] `cd boxr && npm run test:e2e -- bracket.spec.ts` — старые e2e не сломаны (2/2).
- [ ] `cd boxr && npm run test:e2e -- schedule.spec.ts` — 2/2.
